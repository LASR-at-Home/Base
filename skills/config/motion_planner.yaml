/play_motion2_executor:
  ros__parameters:
    motion_planner:
      disable_motion_planning: true

      #TODO: Uncomment and set disable_motion_planning to false once moveit/planner is available (also in skill )

      # planning_groups:
      #   - Something 

      # # Joints where aren't planned
      # exclude_from_planning_joints:
      #   - head_1_joint
      #   - head_2_joint
      #   - torso_lift_joint
      #   - gripper_left_finger_joint
      #   - gripper_right_finger_joint

      # joint_tolerance: 0.01

      # Non-planning approach parameters (used for excluded joints)
      approach_velocity: 0.5
      approach_min_duration: 0.5