version: "3.1"

intents:
  - make_order
  - affirm
  - wake_word
entities:
  - item
  - quantity
  - choice
  - wake
session_config:
  session_expiration_time: 60
  carry_over_slots_to_new_session: true
